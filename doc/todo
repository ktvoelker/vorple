
To-Do
=====

Stop using orphan instances
* Make a newtype wrapper around HTTP statuses
* Make a function that composes throwError with the newtype wrapper
* Make sure that it's possible to catch and react to the specific error

Stop using -XGeneralizedNewtypeDeriving

Replace the tests' dependency on the cookie package with cookie-jar

Write tests
* Positive tests
 * Cookie options
 * Out-of-order use of cookies
 * Environment consistency
 * Send irrelevant cookie
 * Throw error
* Negative tests
 * Send invalid CSRF key
 * Send invalid cookie
 * Send invalid body
 * Use invalid method
 * Use invalid path

Set better default options

Write a README

Write Haddock documentation
* Package description
* Logging functions
* Internal functions

Add better package version dependencies

