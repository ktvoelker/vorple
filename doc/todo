
To-Do
=====

Make a serializer to SQL for the Database DSL

Add more aggregate and string functions to the DSL

Make a TH function that type-checks the SQL against the tables and produces a function
  that executes the query

Add support for custom functions, for which a SQL type declaration must be given

Make logging easier
  Use TH to do printf-style logging, but with special primitives for Show and ToJSON

Parse cookies properly
  URL encoding and decoding
  Support for multiple key-value pairs in the cookie string
  Does Scotty or WAI already do this?

Add a DB module that ensures transactionality of requests

Write tests

Get rid of Scotty?

